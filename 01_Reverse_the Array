class Solution {
  public:
    void reverseArray(vector<int> &arr) {
        // code here
        int n=arr.size()-1;
        int start=0;
        int end=n;
        while(start<=end){
            swap(arr[start],arr[end]);
            start++;
            end--;
        }
        
    }
};
